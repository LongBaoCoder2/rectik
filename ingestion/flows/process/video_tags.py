# from .base import BaseCSVProcess

# class VideoTagsProcessor(BaseCSVProcess):
#     def __init__(self, engine, csv_path):
#         super().__init__(engine, csv_path)

#     def process(self, table):
#         self.df.groupby(["video_id"], as_index=False).max("date")
#         return super().process(table)

